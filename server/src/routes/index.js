const authRoutes = require("./auth");
const userRoutes = require("./user");
const todoRoutes = require("./todo");
const profileRoutes = require("./profile");

module.exports = { authRoutes, userRoutes, todoRoutes, profileRoutes };
